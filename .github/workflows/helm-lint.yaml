# SPDX-FileCopyrightText: Copyright (c) 2026 Red Hat, Inc. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
name: Helm Lint

on:
  push:
    branches: [main]
    paths:
      - 'helm/**'
      - '.github/workflows/helm-lint.yaml'
  pull_request:
    paths:
      - 'helm/**'
  workflow_dispatch:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: Install Helm
        uses: azure/setup-helm@v4

      - name: Add Helm repos
        run: |
          helm repo add temporal https://go.temporal.io/helm-charts
          helm repo add hashicorp https://helm.releases.hashicorp.com
          helm repo add nats https://nats-io.github.io/k8s/helm/charts/
          helm repo update

      - name: Build dependencies
        run: |
          helm dependency build helm/infra-cloud/
          helm dependency build helm/infra-site/

      - name: Lint
        run: |
          helm lint helm/nvidia-infra-controller-prereqs/
          helm lint helm/infra-cloud/
          helm lint helm/infra-site/

      - name: Template (dry-run)
        run: |
          helm template nvidia-infra-controller-prereqs helm/nvidia-infra-controller-prereqs/ > /dev/null
          helm template nvidia-infra-controller-cloud-infra helm/infra-cloud/ > /dev/null
          helm template nvidia-infra-controller-site-infra helm/infra-site/ > /dev/null
